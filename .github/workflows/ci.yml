name: CI

on:
  pull_request:
  push:
    branches: [main]

defaults:
  run:
    working-directory: web-portfolio

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web-portfolio/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run test:run
